# Notas:

Este es mi primer programa en Node

```
Opciones:
--help Muestra ayuda [booleano]
--version Muestra número de versión [booleano]
-b, --base La base para la tabla de multiplicar [número] [requerido]
-l, --listar Listar la tabla de multiplicar en consola
[booleano] [defecto: false]
-h, --hasta Limite maximo hasta donde se va a multiplicar
[número] [defecto: 10]

```
